<section class="flex flex-col w-full mt-8">
    <h2 class="uppercase font-bold"> {{params.title}}</h2>
{% for topic in params.topics %}
<a href="{{topic.link}}" class="hover:bg-slate-600 focus:bg-slate-600 bg-slate-300 hover:text-white focus:text-white my-4">
<article class="flex w-full  p-4 justify-between items-center">   
    <h2 class="text-xl mr-4">{{topic.title}}</h2>
    <p class="text-sm">{{topic.text}}</p>    
</article>
</a>
{% endfor %}
</section>